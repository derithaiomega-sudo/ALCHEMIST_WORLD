# First Vertical Slice

Creator intent → persisted project → canonical context → explicit plan → scoped repository change → validation → documentation → durable memory → change report.

Acceptance criteria are defined before implementation expands.
