# Agent Runtime

Agent = identity + goal + skills + tools + memory scope + permissions + constraints + output contract.

Lifecycle: OBSERVE → CONTEXT → UNDERSTAND → PLAN → POLICY → EXECUTE → VALIDATE → DOCUMENT → REMEMBER → REPORT.

Builder Agent is the first implementation target.
